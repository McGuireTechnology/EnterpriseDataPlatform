# Contrib

This folder is a local cache for external reference documents used to shape EDP target features.

Source materials are intentionally not committed. Use `scripts/Sync-ContribDocuments.ps1` to gather approved local references into this folder when needed.
